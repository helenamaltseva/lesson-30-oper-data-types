const sum = 0.1 + 0.2;
console.log(sum.toFixed(2));

const str = "1";
const num = 2;
const sum = parseInt(str) + num;
console.log(sum);

const flashSize = prompt("16");
const fileSize = 820; 
const numberOfFiles = Math.floor(flashSize * 1024 / fileSize);
console.log(`${numberOfFiles}`);

